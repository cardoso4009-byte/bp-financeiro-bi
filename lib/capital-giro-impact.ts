export type WorkingCapitalImpact={pmrDias:number;pmeDias:number;pmpDias:number;receitaMensal:number;custoMensal:number;caixaImpacto:number;novoCiclo:number}
export function calculateWorkingCapitalImpact(pmrDias:number,pmeDias:number,pmpDias:number,receitaMensal:number,custoMensal:number):WorkingCapitalImpact{const basePmr=30,basePme=30,basePmp=30;const deltaReceber=(pmrDias-basePmr)/30*receitaMensal;const deltaEstoque=(pmeDias-basePme)/30*custoMensal;const deltaFornecedores=(pmpDias-basePmp)/30*custoMensal;const caixaImpacto=-(deltaReceber+deltaEstoque-deltaFornecedores);return{pmrDias,pmeDias,pmpDias,receitaMensal,custoMensal,caixaImpacto,novoCiclo:pmrDias+pmeDias-pmpDias}}