'use client'
import { useEffect, useMemo, useState } from 'react'
import { buildCashForecast, forecastStatus, workingCapitalMetrics, type Scenario } from '@/lib/fluxo-caixa-projetado'
import { initialEntries, type FinancialEntry } from '@/lib/lancamentos-data'
import { readFinancialSource } from '@/lib/financial-source'
const brl=(n:number)=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0})
const months=['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez']
type Kind='inflows'|'operatingOutflows'|'capex'|'financing'
const groups=[{key:'inflows' as Kind,label:'Entradas',children:['Recebimentos de clientes','Outras receitas']},{key:'operatingOutflows' as Kind,label:'Saídas Operacionais',children:['Pessoal','Fornecedores','Impostos','Administrativas','Comerciais']},{key:'capex' as Kind,label:'CAPEX',children:['Equipamentos','Obras','Tecnologia']},{key:'financing' as Kind,label:'Financiamentos',children:['Novos financiamentos','Amortizações','Juros']}]
export default function FluxoCaixa(){const[scenario,setScenario]=useState<Scenario>('base');const[start,setStart]=useState(0);const[end,setEnd]=useState(11);const[expanded,setExpanded]=useState<Set<string>>(new Set());const[entries,setEntries]=useState<FinancialEntry[]>(initialEntries);useEffect(()=>setEntries(readFinancialSource().entries),[]);const all=useMemo(()=>buildCashForecast(scenario),[scenario]);const rows=all.slice(start,end+1);const status=forecastStatus(rows);const wc=useMemo(()=>workingCapitalMetrics(scenario),[scenario]);const actual=useMemo(()=>buildActualCash(entries),[entries]);const min=Math.min(...rows.map(r=>r.closing));const max=Math.max(...rows.map(r=>r.closing));const total=(field:Kind|'net')=>rows.reduce((s,r)=>s+r[field],0);const toggle=(key:string)=>setExpanded(prev=>{const next=new Set(prev);next.has(key)?next.delete(key):next.add(key);return next});return <main className="content" style={{marginLeft:0,width:'100%',maxWidth:1500,margin:'0 auto'}}><header><div><small>PLANEJAMENTO FINANCEIRO</small><h1>Fluxo de Caixa Gerencial</h1><p>Período de análise • Cenários • Alertas de liquidez • Capital de Giro</p></div><div className="period-controls"><label>Período de análise</label><div><select value={start} onChange={e=>{setStart(Number(e.target.value));setExpanded(new Set())}}>{months.map((m,i)=><option key={m} value={i}>Início: {m}</option>)}</select><select value={end} onChange={e=>setEnd(Math.max(start,Number(e.target.value)))}>{months.map((m,i)=><option key={m} value={i}>Fim: {m}</option>)}</select></div><div className="segmented">{(['base','otimista','pessimista'] as Scenario[]).map(s=><button type="button" key={s} className={scenario===s?'selected':''} onClick={()=>setScenario(s)}>{s[0].toUpperCase()+s.slice(1)}</button>)}</div></div></header><div className="cards"><Card title="Caixa Inicial" value={rows[0].opening}/><Card title="Menor Caixa" value={min}/><Card title="Maior Caixa" value={max}/><Card title="Caixa Final" value={rows[rows.length-1].closing}/></div><section className="panel wide"><div className="panel-title"><div><h2>Projeção mensal</h2><span>+ expande • − recolhe • Grupo → categoria → detalhe</span></div><span>{status.status}</span></div><div className="table-wrap"><table><thead><tr><th style={{textAlign:'left'}}>Conta</th>{rows.map(r=><th key={r.month}>{r.month}</th>)}<th>Total</th></tr></thead><tbody><CashLine label="Caixa Inicial" values={rows.map(r=>r.opening)} bold/>{groups.map(g=><CashGroup key={g.key} group={g} rows={rows} expanded={expanded} toggle={toggle}/>)}<CashLine label="Variação de Caixa" values={rows.map(r=>r.net)} bold/><CashLine label="Caixa Final" values={rows.map(r=>r.closing)} bold totalOverride={rows[rows.length-1].closing}/></tbody></table></div></section><section className="panel wide"><div className="panel-title"><div><h2>Orçado × Realizado</h2><span>Comparação mensal de entradas, saídas e variação de caixa</span></div><span>Base financeira</span></div><div className="table-wrap"><table><thead><tr><th style={{textAlign:'left'}}>Indicador</th>{months.slice(start,end+1).map(m=><th key={m}>{m}</th>)}<th>Total</th></tr></thead><tbody><BudgetCashLine label="Entradas" budget={rows.map(r=>r.inflows)} actual={months.slice(start,end+1).map((_,i)=>actual[start+i].inflows)}/><BudgetCashLine label="Saídas Operacionais" budget={rows.map(r=>-r.operatingOutflows)} actual={months.slice(start,end+1).map((_,i)=>-actual[start+i].operatingOutflows)}/><BudgetCashLine label="CAPEX" budget={rows.map(r=>-r.capex)} actual={months.slice(start,end+1).map((_,i)=>-actual[start+i].capex)}/><BudgetCashLine label="Variação de Caixa" budget={rows.map(r=>r.net)} actual={months.slice(start,end+1).map((_,i)=>actual[start+i].net)}/></tbody></table></div><div className="note" style={{marginTop:12}}>O “Orçado” usa a projeção do cenário selecionado. O “Realizado” considera lançamentos efetivamente pagos na base financeira; itens sem pagamento não entram no realizado de caixa.</div></section><section className="panel"><div className="panel-title"><div><h2>Capital de Giro</h2><span>Indicadores que explicam a pressão sobre o caixa</span></div><span>{scenario[0].toUpperCase()+scenario.slice(1)}</span></div><div className="cards"><Metric title="PMR" value={`${wc.pmr} dias`} help="Prazo médio de recebimento"/><Metric title="PME" value={`${wc.pme} dias`} help="Prazo médio de estoque"/><Metric title="PMP" value={`${wc.pmp} dias`} help="Prazo médio de pagamento"/><Metric title="Ciclo Financeiro" value={`${wc.cicloFinanceiro} dias`} help="PMR + PME − PMP"/><Metric title="Necessidade de Capital de Giro" value={brl(wc.necessidadeCapitalGiro)} help="Recebíveis + estoque − fornecedores"/></div><div className="note" style={{marginTop:12}}>Leitura: quanto maior o ciclo financeiro, mais recursos ficam presos na operação. A NCG mostra o capital necessário para sustentar esse ciclo.</div></section><div className="grid"><section className="panel"><div className="panel-title"><h2>Diagnóstico</h2><span>{status.status}</span></div><div className="note"><strong>{status.title}</strong><p>{status.detail}</p><p>Caixa mínimo: <strong>{brl(rows[0].minimum)}</strong>.</p></div></section><section className="panel"><div className="panel-title"><h2>Leitura gerencial</h2><span>Decisão</span></div><div className="rows"><div className="row"><span>Entradas projetadas</span><b>{brl(total('inflows'))}</b></div><div className="row"><span>Saídas operacionais</span><b>{brl(total('operatingOutflows'))}</b></div><div className="row"><span>CAPEX</span><b>{brl(total('capex'))}</b></div><div className="row"><span>Variação acumulada</span><b>{brl(rows[rows.length-1].closing-rows[0].opening)}</b></div><div className="row"><span>Ciclo financeiro</span><b>{wc.cicloFinanceiro} dias</b></div></div></section></div><section className="panel"><div className="panel-title"><h2>Alavancas de caixa</h2><span>Consultoria</span></div><div className="note">Use os cenários para testar recebimentos, custos, CAPEX e financiamentos. A próxima evolução pode simular o impacto de reduzir PMR, PME ou aumentar PMP.</div></section></main>}
function buildActualCash(entries:FinancialEntry[]){return months.map((_,i)=>{const month=`2026-${String(i+1).padStart(2,'0')}`;const paid=entries.filter(e=>e.status==='Pago'&&e.paymentDate?.startsWith(month));const inflows=paid.filter(e=>e.type==='Receita').reduce((s,e)=>s+e.value,0);const operatingOutflows=paid.filter(e=>e.type==='Despesa').reduce((s,e)=>s+e.value,0);const capex=paid.filter(e=>e.type==='CAPEX').reduce((s,e)=>s+e.value,0);const financing=paid.filter(e=>e.type==='Financiamento').reduce((s,e)=>s+e.value,0);return{inflows,operatingOutflows,capex,financing,net:inflows-operatingOutflows-capex+financing}})}
function CashGroup({group,rows,expanded,toggle}:{group:{key:Kind;label:string;children:string[]};rows:any[];expanded:Set<string>;toggle:(key:string)=>void}){const open=expanded.has(group.key);const vals=rows.map(r=>group.key==='operatingOutflows'||group.key==='capex'?-r[group.key]:r[group.key]);return <><tr className="group-row" onClick={()=>toggle(group.key)} style={{cursor:'pointer'}}><td><ExpandIcon open={open} onClick={()=>toggle(group.key)} label={group.label}/><b>{group.label}</b></td>{vals.map((r,i)=><td key={i}>{brl(r)}</td>)}<td><b>{brl(vals.reduce((s:number,v:number)=>s+v,0))}</b></td></tr>{open&&group.children.map((child,i)=><tr key={child} className="item-row"><td style={{paddingLeft:34}}>↳ {child}</td>{vals.map((v,j)=><td key={j} style={{color:'#667085'}}>{brl(v*(i===0?0.52:i===1?0.18:i===2?0.12:i===3?0.10:0.08))}</td>)}<td>—</td></tr>)}</>}
function ExpandIcon({open,onClick,label}:{open:boolean;onClick:()=>void;label:string}){return <button type="button" className="expand-btn" aria-label={open?`Recolher ${label}`:`Expandir ${label}`} onClick={e=>{e.stopPropagation();onClick()}}>{open?'−':'+'}</button>}
function CashLine({label,values,bold,totalOverride}:{label:string;values:number[];bold?:boolean;totalOverride?:number}){const total=totalOverride??values.reduce((s,v)=>s+v,0);return <tr><td>{bold?<b>{label}</b>:label}</td>{values.map((v,i)=><td key={i}>{bold?<b>{brl(v)}</b>:brl(v)}</td>)}<td>{bold?<b>{brl(total)}</b>:brl(total)}</td></tr>}
function BudgetCashLine({label,budget,actual}:{label:string;budget:number[];actual:number[]}){const vars=budget.map((b,i)=>actual[i]-b);return <><tr><td><b>{label}</b></td>{actual.map((v,i)=><td key={i}>{brl(v)}</td>)}<td><b>{brl(actual.reduce((s,v)=>s+v,0))}</b></td></tr><tr><td style={{paddingLeft:18,color:'#667085'}}>↳ Orçado</td>{budget.map((v,i)=><td key={i} style={{color:'#667085'}}>{brl(v)}</td>)}<td style={{color:'#667085'}}>{brl(budget.reduce((s,v)=>s+v,0))}</td></tr><tr><td style={{paddingLeft:18,color:'#667085'}}>↳ Variação</td>{vars.map((v,i)=><td key={i} style={{color:v===0?'#667085':v>0?'#067647':'#b42318'}}>{brl(v)}</td>)}<td style={{color:vars.reduce((s,v)=>s+v,0)>=0?'#067647':'#b42318'}}>{brl(vars.reduce((s,v)=>s+v,0))}</td></tr></>}
function Card({title,value}:{title:string,value:number}){return <div className="card"><span>{title}</span><strong>{brl(value)}</strong><small>projeção</small></div>}
function Metric({title,value,help}:{title:string;value:string;help:string}){return <div className="card"><span>{title}</span><strong>{value}</strong><small>{help}</small></div>}
